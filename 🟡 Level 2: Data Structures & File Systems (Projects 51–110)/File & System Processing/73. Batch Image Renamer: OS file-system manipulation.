#73. 🖼 Batch Image Renamer
import os

folder = "images"
for i, filename in enumerate(os.listdir(folder), 1):
    if filename.endswith(".jpg"):
        new_name = f"image_{i}.jpg"
        os.rename(os.path.join(folder, filename), os.path.join(folder, new_name))
