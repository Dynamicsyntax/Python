# 📊 Statistics Tool

import statistics

numbers = []
count = int(input("Enter count of numbers you want to enter: "))

for i in range(count):
    number = int(input(f"Enter {i+1} number: "))
    numbers.append(number)

print(f"You have entered numbers as: {numbers}")

operations = {
    "sum": sum,
    "max": max,
    "min": min,
    "avg": statistics.mean
}

operation = input("Choose an operation (sum / max / min / avg): ").strip().lower()

if operation in operations:
    result = operations[operation](numbers)
    print(f"{operation} of {numbers} = {result}")
else:
    print("❌ Invalid operation")
